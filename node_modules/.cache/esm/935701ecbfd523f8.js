let Joi,User;_bfd‍.x([["register",()=>register],["login",()=>login],["check",()=>check],["logout",()=>logout]]);_bfd‍.w("@hapi/joi",[["default",["Joi"],function(v){Joi=v}]]);_bfd‍.w("../../models/User",[["default",["User"],function(v){User=v}]]);


       const register = async (ctx) => {
	const schema = Joi.object().keys({
		username: Joi.string().alphanum().min(3).max(20).required(),
		password: Joi.string().required(),
	});
	const result = schema.validate(ctx.request.body);
	if (result.error) {
		ctx.status = 400;
		ctx.body = result.error;
		console.log('occur 400 error');
		return;
	}

	_bfd‍.g.console.log('reqbody : ' + ctx);

	const { username, password } = ctx.request.body;
	try {
		const exists = await User.findByUsername(username);
		if (exists) {
			ctx.status = 409;
			console.log(`exists : 409`);
			return;
		}
		const user = new User({
			username,
		});
		console.log(`user : ${user}`);

		await user.setPassword(password);
		await user.save();

		ctx.body = user.serialize();
		const token = user.generateToken();
		ctx.cookies.set('access_token', token, {
			maxAge: 1000 * 60 * 60 * 24 * 7, //7일
			httponly: true,
		});
	} catch (error) {
		ctx.throw(500, error);
		_bfd‍.g.console.log('log : ' + error);
	}
};

       const login = async (ctx) => {
	const { username, password } = ctx.request.body;

	if (!username || !password) {
		ctx.status = 401;
		console.log('에러 발생 401');
		return;
	}

	try {
		const user = await User.findByUsername(username);
		if (!user) {
			ctx.status = 401;
			console.log('에러 아이디가 없습니다.');
			return;
		}
		const valid = await user.checkPassword(password);
		if (!valid) {
			ctx.status = 401;
			return;
		}
		ctx.body = user.serialize();

		const token = user.generateToken();
		ctx.cookies.set('access_token', token, {
			maxAge: 1000 * 60 * 60 * 24 * 7, //7일
			httponly: true,
		});
	} catch (e) {
		ctx.throw(500, e);
	}
};

       const check = async (ctx) => {
	const { user } = ctx.state;
	if (!user) {
		ctx.status = 401;
		return;
	}
	ctx.body = user;
};

       const logout = async (ctx) => {
	ctx.cookies.set('acess_token');
	ctx.status = 204;
};
