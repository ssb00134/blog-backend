let Joi,User;_869‍.x([["register",()=>register],["login",()=>login],["check",()=>check],["logout",()=>logout]]);_869‍.w("@hapi/joi",[["default",["Joi"],function(v){Joi=v}]]);_869‍.w("../../models/User",[["default",["User"],function(v){User=v}]]);


       const register = async (ctx) => {
	const schema = Joi.object().keys({
		username: Joi.string().alphanum().min(3).max(20).required(),
		password: Joi.string().required(),
	});
	const result = schema.validate(ctx.request.body);
	if (result.error) {
		ctx.status = 400;
		ctx.body = result.error;
		console.log('occur 400 error');
		return;
	}

	_869‍.g.console.log('reqbody : ' + ctx);

	const { username, password } = ctx.request.body;
	try {
		const exists = await User.findByUsername(username);
		if (exists) {
			ctx.status = 409;
			console.log(`exists : 409`);
			return;
		}
		const user = new User({
			username,
		});
		console.log(`user : ${user}`);

		await user.setPassword(password);
		await user.save();

		const data = user.toJSON();

		delete data.hashedPassword;
		ctx.body = data;
	} catch (error) {
		ctx.throw(500, error);
		_869‍.g.console.log('log : ' + error);
	}
};

       const login = async (ctx) => {};

       const check = async (ctx) => {};

       const logout = async (ctx) => {};
